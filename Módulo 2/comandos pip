comando pip install serve para instalar libs

o comando pip freeze mostra as libs que estao sendo utilizadas naquele projeto
específico

pip freeze -l > nome.txt serve para criar um arquivo .txt que lista as libs
que estao sendo utilizadas naquele projeto, podendo assim facilitar o download
das libs pelo pip

pip install -r nome.txt serve para automatizar o processo de instalação das
libs